name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: mesh2param-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    permissions:
      contents: read
    uses: esaueng/Mesh2Param/.github/workflows/fleet-ci.yml@f146a3c6db253ffc381623649672f0e82e95c1d6
