name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: mesh2param-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    permissions:
      contents: read
    uses: esaueng/Mesh2Param/.github/workflows/fleet-ci.yml@3396db6bcee9362837f639f3c78052abde5cf302
