name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: mesh2param-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    uses: esaueng/Mesh2Param/.github/workflows/ci-jobs.yml@396776854fb84905e090962ae9344f65bd6e12fd
