name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: mesh2param-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    permissions:
      contents: read
    uses: esaueng/Mesh2Param/.github/workflows/fleet-ci.yml@bbb45854cfcd8c979e9597dfc8f869c22ffe54c3
