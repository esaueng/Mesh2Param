{
  "name": "mesh2param",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic mesh-to-parametric CAD reconstruction workspace",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=22.22.0",
    "pnpm": ">=10"
  },
  "scripts": {
    "build": "pnpm build:web && uv build",
    "build:packages:js": "pnpm --filter @mesh2param/contracts build && pnpm --filter @mesh2param/ui build",
    "build:packages": "pnpm build:packages:js && pnpm core:wasm",
    "core:wasm": "bash scripts/build_core_wasm.sh",
    "build:web": "pnpm build:packages && pnpm --filter @mesh2param/web build",
    "cf:types": "wrangler types",
    "cf:typecheck": "tsc -p tsconfig.cloudflare.json",
    "bundle:budgets": "node scripts/check_bundle_budgets.mjs",
    "cf:assets": "node scripts/check_cloudflare_assets.mjs",
    "cf:check": "pnpm test:worker && pnpm build:web && pnpm cf:assets && pnpm cf:types && pnpm cf:typecheck && wrangler deploy --dry-run",
    "cf:dev": "pnpm build:web && pnpm cf:assets && pnpm cf:types && pnpm cf:typecheck && wrangler dev",
    "cf:test": "pnpm --filter @mesh2param/web test:cloudflare",
    "cf:deploy": "pnpm build:web && pnpm cf:assets && pnpm cf:types && pnpm cf:typecheck && wrangler deploy",
    "db:migrate": "uv run --frozen python -m mesh2param_api.migrate",
    "dev": "concurrently --kill-others --names api,web --prefix-colors blue,cyan \"pnpm dev:api\" \"pnpm dev:web\"",
    "dev:api": "uv run --frozen mesh2param serve --reload",
    "dev:web": "pnpm --filter @mesh2param/web dev",
    "typecheck": "pnpm --filter @mesh2param/contracts typecheck && pnpm --filter @mesh2param/ui typecheck && pnpm --filter @mesh2param/core-wasm typecheck && pnpm --filter @mesh2param/web typecheck && uv run --frozen --extra dev mypy engine services scripts tests",
    "lint": "pnpm --filter @mesh2param/contracts generate:check && pnpm --filter @mesh2param/core-wasm lint && pnpm --filter @mesh2param/web lint && uv run --frozen --extra dev ruff check engine services scripts tests",
    "test:frontend": "pnpm test:worker && pnpm --filter @mesh2param/contracts test && pnpm --filter @mesh2param/ui test && pnpm --filter @mesh2param/core-wasm test && pnpm --filter @mesh2param/web test",
    "test:backend": "uv run --frozen --extra dev pytest",
    "test:api": "uv run --frozen --extra dev pytest tests/api",
    "test:geometry": "uv run --frozen --extra dev pytest tests/test_m2_reconstruction.py::test_geometry_acceptance_exact_sixteen_step_sequence",
    "test": "pnpm test:frontend && pnpm test:backend",
    "test:e2e": "pnpm --filter @mesh2param/web test:e2e",
    "samples:generate": "bash scripts/samples_container.sh generate",
    "samples:check": "bash scripts/samples_container.sh check",
    "curved:fixtures": "uv run --frozen python scripts/generate_curved_fixtures.py",
    "curved:baseline": "uv run --frozen python scripts/run_curved_baseline.py",
    "general:fixtures": "uv run --frozen python scripts/generate_general_fixtures.py",
    "general:baseline": "uv run --frozen python scripts/run_general_baseline.py",
    "corpus:generate": "uv run --frozen python -m scripts.real_corpus generate",
    "corpus:index": "uv run --frozen python -m scripts.real_corpus index",
    "licenses:check": "uv run --frozen --extra dev python scripts/check_licenses.py --check",
    "mesh2param": "uv run --frozen mesh2param",
    "verify": "pnpm build:packages && pnpm typecheck && pnpm lint && pnpm test && pnpm test:geometry && pnpm samples:check && pnpm build && pnpm test:e2e && pnpm licenses:check",
    "acceptance": "uv run --frozen --extra dev python scripts/run_acceptance.py",
    "test:worker": "node --test cloudflare/*.test.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^26.1.1",
    "concurrently": "^10.0.3",
    "typescript": "^5.9.3",
    "wrangler": "^4.112.0"
  }
}
