{
  "name": "mesh2param",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic mesh-to-parametric CAD reconstruction workspace",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "scripts": {
    "build": "pnpm --filter @mesh2param/contracts build && XDG_CACHE_HOME=.cache uv build",
    "typecheck": "pnpm --filter @mesh2param/contracts typecheck && XDG_CACHE_HOME=.cache uv run --extra dev mypy engine scripts tests",
    "lint": "pnpm --filter @mesh2param/contracts generate:check && XDG_CACHE_HOME=.cache uv run --extra dev ruff check engine scripts tests",
    "test": "pnpm --filter @mesh2param/contracts test && XDG_CACHE_HOME=.cache uv run --extra dev pytest",
    "samples:generate": "XDG_CACHE_HOME=.cache uv run mesh2param samples generate",
    "mesh2param": "XDG_CACHE_HOME=.cache uv run mesh2param",
    "verify": "pnpm typecheck && pnpm lint && pnpm test && pnpm build && pnpm samples:generate"
  }
}
