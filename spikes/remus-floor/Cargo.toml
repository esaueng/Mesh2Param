[package]
name = "remus-floor"
version = "0.1.0"
edition = "2024"
publish = false

# Standalone: not a member of the Mesh2Param tree nor of the remus workspace.
[workspace]

[dependencies]
remus-io = {path = "../../../../../../remus/crates/io"}
remus-operations = {path = "../../../../../../remus/crates/operations"}
remus-topology = {path = "../../../../../../remus/crates/topology"}
serde_json = "1"

[profile.release]
debug = false
