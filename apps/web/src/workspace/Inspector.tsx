import type { WorkspaceActions, WorkspaceViewModel } from "./types";
import { ImportPanel } from "../inspector/ImportPanel";import { RepairPanel } from "../inspector/RepairPanel";import { SurfacesPanel } from "../inspector/SurfacesPanel";import { FeaturesPanel } from "../inspector/FeaturesPanel";import { RefinePanel } from "../inspector/RefinePanel";import { ValidatePanel } from "../inspector/ValidatePanel";import { ExportPanel } from "../inspector/ExportPanel";
export function Inspector({vm,actions}:{vm:WorkspaceViewModel;actions:WorkspaceActions}){return <aside className="inspector" aria-label={`${vm.activeStep} inspector`}>{vm.activeStep==="import"?<ImportPanel vm={vm} actions={actions}/>:vm.activeStep==="repair"?<RepairPanel vm={vm} actions={actions}/>:vm.activeStep==="surfaces"?<SurfacesPanel vm={vm} actions={actions}/>:vm.activeStep==="features"?<FeaturesPanel vm={vm} actions={actions}/>:vm.activeStep==="refine"?<RefinePanel vm={vm} actions={actions}/>:vm.activeStep==="validate"?<ValidatePanel vm={vm} actions={actions}/>:<ExportPanel vm={vm} actions={actions}/>}</aside>}
