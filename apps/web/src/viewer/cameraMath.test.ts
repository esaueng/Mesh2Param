import {describe,expect,it} from "vitest";import * as THREE from "three";import {cameraFitForBox,shouldFitCamera} from "./cameraMath";
describe("camera stability",()=>{it("fits only for new content or an explicit fit command",()=>{expect(shouldFitCamera("a","a",2,2)).toBe(false);expect(shouldFitCamera("a","b",2,2)).toBe(true);expect(shouldFitCamera("a","a",3,2)).toBe(true)});it("centers a finite camera pose",()=>{const fit=cameraFitForBox(new THREE.Box3(new THREE.Vector3(0,0,0),new THREE.Vector3(60,40,45)),42,16/9);expect(fit.target.toArray()).toEqual([30,20,22.5]);expect(fit.position.toArray().every(Number.isFinite)).toBe(true)})});
