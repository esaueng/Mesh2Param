import * as THREE from "three";

export type ViewPreset="iso"|"front"|"back"|"left"|"right"|"top"|"bottom";
export interface CameraFit { position:THREE.Vector3; target:THREE.Vector3; up:THREE.Vector3 }
const DIRECTIONS:Record<ViewPreset,[number,number,number]>={iso:[1.15,-1.35,.95],front:[0,-1,0],back:[0,1,0],left:[-1,0,0],right:[1,0,0],top:[0,0,1],bottom:[0,0,-1]};
export function cameraFitForDirection(box:THREE.Box3,fovDegrees:number,aspect:number,viewDirection:[number,number,number],margin=1.2):CameraFit{const center=box.getCenter(new THREE.Vector3());const size=box.getSize(new THREE.Vector3());const radius=Math.max(size.length()/2,1e-3);const vertical=Math.tan(THREE.MathUtils.degToRad(fovDegrees/2));const horizontal=vertical*Math.max(aspect,.1);const distance=margin*radius/Math.max(Math.min(vertical,horizontal),.05);const direction=new THREE.Vector3(...viewDirection).normalize();const worldUp=new THREE.Vector3(0,0,1);const up=Math.abs(direction.z)>.98?new THREE.Vector3(-1,0,0):worldUp.clone().projectOnPlane(direction).normalize();return{position:center.clone().addScaledVector(direction,distance),target:center,up}}
export function cameraFitForBox(box:THREE.Box3,fovDegrees:number,aspect:number,preset:ViewPreset="iso",margin=1.2):CameraFit{const center=box.getCenter(new THREE.Vector3());const size=box.getSize(new THREE.Vector3());const radius=Math.max(size.length()/2,1e-3);const vertical=Math.tan(THREE.MathUtils.degToRad(fovDegrees/2));const horizontal=vertical*Math.max(aspect,.1);const distance=margin*radius/Math.max(Math.min(vertical,horizontal),.05);const direction=new THREE.Vector3(...DIRECTIONS[preset]).normalize();const up=Math.abs(direction.z)>.98?new THREE.Vector3(0,1,0):new THREE.Vector3(0,0,1);return{position:center.clone().addScaledVector(direction,distance),target:center,up}}
export function shouldFitCamera(previousArtifactKey:string|null,nextArtifactKey:string,explicitFitRevision:number,appliedFitRevision:number){return previousArtifactKey!==nextArtifactKey||explicitFitRevision!==appliedFitRevision}
